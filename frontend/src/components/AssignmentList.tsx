export function AssignmentList({ onSelectAssignment }: { onSelectAssignment: (id: string) => void }) {
  return (
    <div className="flex-1 p-6">
      <h2 className="text-2xl font-bold mb-4">Список поручений</h2>
      <p className="text-muted-foreground">Компонент списка поручений в разработке...</p>
    </div>
  )
}
